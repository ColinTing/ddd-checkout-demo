spring:
  application:
    name: ddd-checkout-demo
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ddd_checkout_demo}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:112233}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
